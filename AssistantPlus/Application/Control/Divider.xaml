<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:community="using:CommunityToolkit.WinUI"
	xmlns:communityControls="using:CommunityToolkit.WinUI.Controls"
	xmlns:local="using:AssistantPlus"
	xmlns:my="using:AssistantPlus.Control"
	>
	<Style TargetType="my:Divider">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="my:Divider">
					<my:Box
						HorizontalAlignment="Stretch"
						VerticalAlignment="Stretch"
						>
						<Rectangle
							HorizontalAlignment="Stretch"
							VerticalAlignment="Stretch"
							MinWidth="{StaticResource DividerThickness}"
							MinHeight="{StaticResource DividerThickness}"
							Fill="{ThemeResource DividerStrokeColorDefaultBrush}"
							/>
					</my:Box>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
</ResourceDictionary>
