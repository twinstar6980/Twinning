<UserControl
	x:Class="Helper.Module.ModuleLauncher.SettingItemPanel"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:community="using:CommunityToolkit.WinUI"
	xmlns:communityControls="using:CommunityToolkit.WinUI.Controls"
	xmlns:local="using:Helper"
	xmlns:localModuleCommandSender="using:Helper.Module.CommandSender"
	xmlns:my="using:Helper.CustomControl"
	mc:Ignorable="d"
	x:DefaultBindMode="OneWay"
>
	<Border
		Margin="0,0" Padding="16,8"
		BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,0,1"
		HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
	>
		<my:MStack Padding="0,0" Orientation="Horizontal" Spacing="16">
			<FontIcon Margin="0,5,0,0" Width="22" Height="22" VerticalAlignment="Top" FontSize="{StaticResource LargeIconSize}" Glyph="{x:Bind Controller.uIcon_Glyph}" />
			<TextBlock Margin="0,7,0,0" my:MStack.Weight="1" HorizontalAlignment="Left" VerticalAlignment="Top" Style="{ThemeResource BodyStrongTextBlockStyle}" LineHeight="20" TextWrapping="NoWrap" Text="{x:Bind Controller.uTitle_Text}" />
			<ContentPresenter Margin="0,0,0,0" my:MStack.Weight="1" HorizontalAlignment="Stretch" VerticalAlignment="Center" Content="{x:Bind Controller.uAction_Content}" />
		</my:MStack>
	</Border>
</UserControl>
